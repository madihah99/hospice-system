<!-- views/manager-dashboard.mustache -->
<div class="manager-dashboard-container">
    <h1>Manager Dashboard</h1>

    <!-- Add Volunteer and View Forms buttons -->
    <div class="manager-actions">
        <form action="/add-volunteer" method="GET">
            <button class="btn-manager-action" type="submit">Add Volunteer</button>
        </form>
        <form action="/view-forms" method="GET">
            <button class="btn-manager-action" type="submit">View Forms</button>
        </form>
    </div>

    <!-- List of Current Volunteers -->
    <h2>Current Volunteers</h2>
    <div class="volunteers-list">
        {{#volunteers}}
        <div class="volunteer-card">
            <img src="/images/{{image}}" alt="{{firstName}} {{lastName}}">
            <div class="volunteer-info">
                <
